<!DOCTYPE html>
<html>
    <head>
        {! load CSS !}
        {@eq key=pageName value="Index"}
            {>css-main/}
        {:else}
            {>css-default/}
        {/eq}

        <title>{title}</title>
    </head>

    <body>
        {! Set up the header. The main has links, default does not !}
        {@eq key=pageName value="Index"}
            {>header-main/}
        {:else}
            {>header-default/}
        {/eq}

        {! Load corn tents !}
    </body>
<html>
